(function (b) {
    var d = {interval: 50, timeout: 350, className: "blink"};
    b.fn.mkBlink = function (e) {
        var c = b.extend({}, d, e), a = this;
        b.each(c, function (c, b) {
            if ("function" === typeof b)
                a.one(c + ".mkBlink", function () {
                    return b(a)
                })
        });
        a.trigger("before.mkBlink");
        a.init = function () {
            var b = void 0, b = setInterval(function () {
                a.toggleClass(c.className)
            }, c.interval);
            setTimeout(function () {
                clearInterval(b);
                a.hasClass(c.className) && a.toggleClass(c.className);
                a.trigger("callback.mkBlink")
            }, c.timeout)
        };
        a.init();
        return a;
    };
})(jQuery);